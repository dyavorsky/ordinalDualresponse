% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgp.R
\name{dr_simulate}
\alias{dr_simulate}
\title{Simulate the ordinal dual-response behavioral process}
\usage{
dr_simulate(
  design,
  Bmat,
  cut,
  model = c("A", "B"),
  seed,
  model2 = c("inclusive", "2max"),
  lambda = 1,
  shock = c("gumbel", "normal"),
  drift = NULL
)
}
\arguments{
\item{design}{a design from \code{\link[=dr_panel_design]{dr_panel_design()}} (use N = 1 for pooled
aggregate data)}

\item{Bmat}{N x P matrix of individual coefficients}

\item{cut}{common cut-point vector (length W - 1, utility scale) or an
N x (W - 1) matrix for heterogeneous cut points. For Model A, utility
cuts map to probability cuts by alpha = exp(-exp(-cut)).}

\item{model}{"A" (probability report, Gumbel link) or "B" (graded
comparison, logistic link)}

\item{seed}{RNG seed}

\item{model2}{"inclusive" (the paper's second stage) or "2max" (DR-2Max
style: latent is the chosen option's deterministic utility plus fresh
logistic noise)}

\item{lambda}{slope on the inclusive value in the second-stage latent
(Model B semantics; 1 is the theoretical value)}

\item{shock}{"gumbel" or "normal" (variance-matched; for misspecification
studies)}

\item{drift}{NULL, \code{list(type = "linear", delta =)} or
\code{list(type = "update", rho =)} for task-order drift in the effective cut
points}
}
\value{
a data list consumable by \code{\link[=dr_mle]{dr_mle()}} and \code{\link[=dr_hb]{dr_hb()}}
}
\description{
Simulate the ordinal dual-response behavioral process
}
